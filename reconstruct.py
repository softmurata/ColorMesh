# pretrain decoder 3d and texture decoder
# pretrain RenderNet
# image(b, 3, 512, 512), shape parameters(b, 3), texture_in(b, 199), vector_in(z_dim)(b, z_dim), light_in(b, 1)
# reconstruction learning
# shape params, texture_in, vector_in, light_in => update weight by Gradient descent(optimization)



